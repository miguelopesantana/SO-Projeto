#include "header.h"

int main(int argc, char *argv[]){
    struct tarefa Task;

    //ler o comando do user
    //guardar os elementos do comando e guardar na struct
    tasks -> num_pedidos = argv[4];
    tasks -> time_betw_tasks = argv[5];
    tasks -> num_tasks = argv[6];
    tasks -> max_time = argv[7];
}